import { createSlice } from "@reduxjs/toolkit";

export const productoSlice = createSlice({
  name: 'producto',
  initialState: {
    productos: [ ],
    activeProduct: null,
  },

  reducers: {

    
  }
});